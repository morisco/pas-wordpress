<div class="modal__content__questions">
  <div class="modal__content__questions__inner">
    {% for faq in faqs %}
      <div class="modal__content__question">
        <h5>{{faq.question}}</h5>
        <div class="excerpt">{{faq.excerpt|raw}}</div>
        <div class="modal__content__question__expanded">
          <button class="modal__content__question__close">
            Back
          </button>
          <h5>{{faq.question}}</h5>
          <div class="modal__content__question__expanded__answer">
          {{faq.answer | raw}}
          </div>
        </div>
        {% if faq.answer %}
          <a href="#"class="question-learn-more">→ learn more</a>
        {% endif %}
      </div>
    {% endfor %}
  </div>
  <div class="modal__content__overlay"></div>
</div>